{
  "devDependencies": {
    "commitizen": "^4.3.0",
    "cz-customizable": "^7.0.0",
    "husky": "^8.0.3"
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitizen --hook || echo 'Please run `git commit` using Commitizen'"
    }
  }
}
